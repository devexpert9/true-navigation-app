import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.truenavigation.app',
  appName: 'True Navigation',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
